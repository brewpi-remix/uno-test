#ifndef _SUPPORT_H
#define _SUPPORT_H

#include <ArduinoLog.h>
#include <Arduino.h>

#include "config.h" // Should be after Arduino.h

void setSerial();
void _delay(unsigned long);

#endif // _SUPPORT_H
